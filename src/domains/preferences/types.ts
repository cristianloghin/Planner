/**
 * This user's settings for this account.
 *
 * The client already validates the stored document into the shape the app
 * wants, so these are its declarations rather than a second copy to drift from.
 */
export type { Preferences, WeekLayout } from '../../client/preferences'
