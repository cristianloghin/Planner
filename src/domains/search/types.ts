/**
 * Search results.
 *
 * The database returns these ready to show, so these are the client's own
 * declarations rather than a second copy that would drift from them.
 */
export type { EventSearchResult } from '../../client/search'
